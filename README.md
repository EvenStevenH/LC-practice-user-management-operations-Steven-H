# Practice: SQL User Management Operations

We created a schema 'testdb' and created a table called 'users'. We then wrote SQL queries to INSERT, UPDATE, and DELETE data, as well as SELECT queries with conditions, sorting and limits (WHERE, OR, ORDER BY, LIMIT, OFFSET). This exercise was very straight forward, where the challenging part was mostly figuring out the tabs and options provided by MY SQL workbench's UI.
